apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: echo
spec:
  type: conversation.echo
  version: v1
  metadata:           # <--- This section must exist
  - name: unused      # Some components require at least one entry
    value: "none"