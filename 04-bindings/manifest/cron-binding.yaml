apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: heartbeat-cron
spec:
  type: bindings.cron
  version: v1
  metadata:
    - name: schedule
      value: "@every 1m" # Triggers every minute
    - name: direction
      value: "input"